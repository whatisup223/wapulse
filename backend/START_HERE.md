# 🚀 خطوات تشغيل Backend - للمبتدئين

## ✅ الخطوة 1: تثبيت المكتبات

افتح Terminal في VS Code واكتب:

```bash
cd backend
npm install
```

⏱️ **انتظر:** 2-3 دقائق

---

## ✅ الخطوة 2: تشغيل الـ Backend

```bash
npm run dev
```

✅ **لو شفت الرسالة دي يبقى تمام:**
```
🚀 WAPulse Backend Server
📡 Running on port 3001
✅ Server started successfully!
```

---

## ⚠️ ملحوظة مهمة:

Backend دلوقتي **بيشتغل بدون قاعدة بيانات** عشان التجربة.

لو عايز تضيف قاعدة بيانات حقيقية، هتحتاج:
1. تثبيت PostgreSQL
2. تعديل DATABASE_URL في ملف `.env`
3. تشغيل `npm run db:push`

---

## 🧪 اختبار Backend:

افتح المتصفح على:
```
http://localhost:3001
```

لو شفت رسالة JSON يبقى Backend شغال! ✅

---

## 📝 الخطوات الجاية:

1. ✅ Backend شغال محلياً
2. ⏳ رفع Backend على EasyPanel
3. ⏳ ربط Frontend بـ Backend
4. ⏳ إضافة قاعدة بيانات

---

## 🆘 لو حصلت مشكلة:

### المشكلة: "Port 3001 already in use"
**الحل:** غير PORT في ملف `.env` لـ 3002

### المشكلة: "Cannot find module"
**الحل:** امسح `node_modules` واعمل `npm install` تاني

### المشكلة: أي حاجة تانية
**الحل:** ابعتلي الرسالة اللي ظهرت!

---

## ✨ كل حاجة تمام؟

لو Backend شغال، يبقى جاهز للرفع على EasyPanel! 🚀
