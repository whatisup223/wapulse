# 🔔 نظام الإشعارات - ملخص سريع

## الأحداث التي تُرسل إشعارات:

### 1️⃣ رسائل WhatsApp الجديدة
```
📱 رسالة جديدة من أحمد محمد
   "مرحباً، أريد الاستفسار عن المنتج..."
```
- **متى**: عند استقبال رسالة من عميل
- **الشرط**: الرسالة ليست منك (!message.fromMe)
- **الصفحة**: Inbox

### 2️⃣ حالة الحملات التسويقية
```
✅ اكتملت الحملة
   "عرض رمضان" - تم الإرسال لـ 150 عميل
```
- **متى**: عند اكتمال/فشل/نجاح حملة
- **الحالات**: completed | success | failed
- **الصفحة**: CreateCampaign

### 3️⃣ مشاكل الاتصال
```
⚠️ انقطع اتصال WhatsApp
   يرجى التحقق من الاتصال وإعادة المحاولة
```
- **متى**: عند انقطاع الاتصال أو انتهاء QR Code
- **الصفحة**: Connection

---

## كيف تعمل؟

### التدفق الكامل:

```
1. المستخدم يفعّل "إشعارات سطح المكتب" من الإعدادات
   ↓
2. المتصفح يطلب الإذن (نافذة منبثقة)
   ↓
3. المستخدم يوافق على الإذن
   ↓
4. يتم حفظ الإعداد في قاعدة البيانات
   ↓
5. عند حدوث أي حدث (رسالة جديدة، اكتمال حملة، إلخ):
   - التطبيق يتحقق: هل الإشعارات مفعّلة؟
   - إذا نعم → يُرسل إشعار للمتصفح
   - إذا لا → لا يُرسل شيء
   ↓
6. الإشعار يظهر في نظام التشغيل (Windows/Mac/Linux)
   ↓
7. الإشعار يختفي تلقائياً بعد 5 ثوانٍ
```

---

## مثال عملي - رسالة جديدة:

```typescript
// في صفحة Inbox
import { sendNewMessageNotification, checkNotificationSettings } from '@/utils/notifications';

// عند استقبال رسالة جديدة
const handleNewMessage = async (message) => {
  // 1. تحديث الواجهة
  setMessages(prev => [...prev, message]);
  
  // 2. التحقق: هل الإشعارات مفعّلة؟
  const enabled = await checkNotificationSettings();
  
  // 3. التحقق: هل الرسالة من عميل (ليست مني)؟
  if (enabled && !message.fromMe) {
    // 4. إرسال الإشعار
    sendNewMessageNotification(
      'أحمد محمد',                    // اسم المرسل
      'مرحباً، أريد الاستفسار...',    // نص الرسالة
      'ar'                             // اللغة
    );
  }
};
```

**النتيجة**: 
- يظهر إشعار في نظام التشغيل
- حتى لو كان المستخدم في تبويب آخر
- حتى لو كان المتصفح مصغّر

---

## الملفات المطلوبة:

### ✅ تم إنشاؤها:
1. `utils/notifications.ts` - وظائف مساعدة للإشعارات
2. `pages/Settings.tsx` - صفحة الإعدادات مع تفعيل الإشعارات
3. `NOTIFICATIONS_GUIDE.md` - دليل شامل

### 📝 تحتاج تعديل (لتطبيق الإشعارات):
1. `pages/Inbox.tsx` - إضافة إشعارات الرسائل الجديدة
2. `pages/CreateCampaign.tsx` - إضافة إشعارات الحملات
3. `pages/Connection.tsx` - إضافة إشعارات مشاكل الاتصال

---

## الخطوات التالية:

### المرحلة 1: اختبار النظام الحالي ✅
- [x] تفعيل الإشعارات من الإعدادات
- [x] طلب إذن المتصفح
- [x] إشعار تجريبي يظهر

### المرحلة 2: تطبيق في Inbox (الأهم)
- [ ] استيراد `utils/notifications`
- [ ] إضافة `checkNotificationSettings()`
- [ ] إرسال إشعار عند رسالة جديدة

### المرحلة 3: تطبيق في CreateCampaign
- [ ] استيراد `utils/notifications`
- [ ] إرسال إشعار عند اكتمال الحملة

### المرحلة 4: تطبيق في Connection
- [ ] استيراد `utils/notifications`
- [ ] إرسال إشعار عند مشكلة اتصال

---

## أسئلة شائعة:

**س: هل الإشعارات تعمل عند إغلاق التطبيق؟**
ج: لا، حالياً تعمل فقط عندما يكون التطبيق مفتوح (في تبويب). لجعلها تعمل عند الإغلاق، نحتاج Service Worker (مستقبلاً).

**س: هل يمكن تخصيص صوت الإشعار؟**
ج: نعم، يمكن إضافة خاصية `sound` في options الإشعار.

**س: ماذا لو رفض المستخدم الإذن؟**
ج: سيظهر تنبيه يطلب منه تفعيل الإذن من إعدادات المتصفح.

**س: هل الإشعارات تعمل على الموبايل؟**
ج: نعم، على Android في Chrome/Firefox. على iOS، الدعم محدود.
